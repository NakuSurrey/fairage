# Streamlit demo app — UI layer that talks to the FastAPI service.
# kept separate from src/ so the production API container does not
# install streamlit, and the demo container does not install onnxruntime.
